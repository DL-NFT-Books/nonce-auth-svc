allOf:
  - $ref: "#/components/schemas/JwtPairKey"
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - session_token
          - refresh_token
        properties:
          session_token:
            $ref: '#/components/schemas/Jwt'
          refresh_token:
            $ref: '#/components/schemas/Jwt'
